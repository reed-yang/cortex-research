# Cortex Research 0.1.21

Status: deployed; same-schema upgrade and installed ingestion acceptance passed.
Recorded 2026-09-14 UTC.

## Changes

- Recover transient arXiv export-API failures through a product-bound HTTPS abs
  endpoint. Validate paper identity and required citation metadata before use.
- Keep metadata failures known and retain both endpoint errors if recovery fails.
- Exclude preexisting unrelated engine processes from the child's survivor scan
  so they cannot invalidate an otherwise successful effect.
- Run the provider-free engine and research suites in hosted checks.

## Installed composition

- Built source: `3649808018abf3ff08ac97a497fff140ee0ef0d5`.
- Installation: `cortex-research-21-a9bfff24fe3bf248`.
- Web build: `cortex-research-21-build-3649808`.
- Predecessor: sequence 20, retained with a fresh state archive and paired proof.
- Control schema 19 and Hermes 0.15.0-gen9 are unchanged.

## Validation and limits

The real child acceptance exercised four failed API attempts, abs fallback,
HTML parsing, corpus writes and indexing against a loopback fixture. The related
engine/research suite passed 320 tests across the full run and one corrected
assertion rerun; Control, artifact and release-record checks passed 385 tests.

The sealed bundle passed both verifiers and a fresh installation test using its
embedded interpreter. A real child recovered four loopback API 429s, parsed HTML
and indexed seven chunks despite an unrelated older process. Permanent metadata
failure remained a known refusal. No external service was called in this test.

The production upgrade preserved the configured native-session bindings, worker
pointer and configuration digest. The new Web reports healthy, the worker is
bound, and the original runtime and Telegram activation gates are restored.
Fresh model turns, message delivery and public identity-provider navigation were
not repeated for this patch. No failed capture was automatically resubmitted.

Product 0.1.21 adds no dependency or credential requirement.
PDF-only ingestion remains outside the supported capability. This release does
not implement cross-process or cross-machine rate coordination and does not
claim to eliminate external arXiv capacity failures.
