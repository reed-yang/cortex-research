# Cortex Research 0.1.20

Status: production cutover accepted; public publication and hosted CI in progress.
Validation recorded2026-09-14 UTC (2026-09-13 in Chicago).

## Composition

- Product0.1.20; installation sequence20; Control schema19, unchanged.
- Built source: `85e0e612884e9a4b71f02dd68b0933b87433130b`.
- Web build: `cortex-research-20-build-85e0e61`.
- Qualified Hermes runtime: `hermes-0.15.0-gen9`, unchanged and supplied separately.
- Target exercised: macOS arm64. The bundle is an unsigned developer distribution.
- Product-side Python distributions:160 ->13; measured wheel bytes:310 MB ->10.2 MB.
  The separate Hermes interpreter/dependencies are excluded from those counts.

## Validation

The extraction campaign passed clean-clone Python/Web tests, bundle verification,
installation/uninstallation and a predecessor upgrade/write/rollback exercise.
The admitted shutdown compatibility fix passed75 lifecycle tests, followed by
three real-process coordinator regressions. Final release identity tests:18 passed.
The final version also passes `uv lock --check`.

The deployed successor was installed through the official lifecycle after a
stopped Media state backup and paired recovery proof. The predecessor remains
retained. All seven native bindings and the qualified worker pointer survived.

Fresh live checks:

- Two existing-thread Web API research turns completed with cited, committed
  Outputs. The same native session retained its historical message prefix and
  appended both user/assistant pairs.
- A third research turn was sent from the actual installed Web UI and completed
  with rendered LaTeX. Preview/Source and folded provenance were exercised.
- A real Telegram `/open`, research request and follow-up were received. Both
  research runs completed and their terminal replies were delivered; the
  poller recorded zero failures or worker-busy responses during this check.
- The deep link names the configured HTTPS origin and exact workspace/thread.

The browser above accessed the production process over an authenticated SSH
loopback tunnel. The public front door reaches its identity provider, but the
automated browser encounters a Cloudflare login verification failure. Therefore
authenticated public navigation is untested. The operator explicitly waived
that interactive login check for this release after the local production Web
passed. The live tunnel ingress targets that same loopback service and its
connector is running. Hosted CI is a separate publication check.

## Distribution boundaries

The sealed controller bundle includes its CPython supply and built Web payload.
Installation takes an explicit host-supplied compatible Node executable. Hermes
is provisioned independently; no operator worker slot, credentials, sessions,
research data or deployment archive is included in a public asset.

Binary packaging retains the sealed bundle unchanged and places supplemental
third-party notices beside it. Package license declarations without a recovered
upstream notice are identified explicitly; standard license reference text is
labelled as such and no copyright holder/year is invented.

No license has yet been selected for Cortex Research's own code. Public source
visibility must not be described as a selected open-source license.

## Functional boundary

`/research` conducts research over adopted evidence and retained dossiers. It
does not restart autonomous legacy idea/exploration engines, introduce a second
profile, or upgrade Hermes. Cross-generation activation and native continuity
remain separate acceptance gates for any future worker upgrade.
