# Cortex Research 0.1.22

Status: deployed; source, installed and read-only live acceptance passed.
Recorded 2026-09-14 UTC.

## Behavior

The default path reads metadata from the direct HTTPS abs page and full text
from the direct HTML page. A valid abs response makes zero export API calls.
If the abs request fails or its metadata fails validation, the export API is
used as a fallback. Both endpoint failures remain a known metadata refusal
before corpus writes, with both causes reported. API error feeds or mismatched
paper identities are rejected.

The existing HTTP client performs direct downloads; no curl runtime dependency
is added. The abs page is attempted once before the API retry budget. The
existing HTML conversion preserves its math and figure handling. PDF-only
papers remain unsupported by strict product ingestion; no OCR support or
archive-only source state is introduced.

## Validation and composition

326 engine/research tests passed, including real child ingestion and indexing
with a deliberately unavailable API and zero API requests on the normal path.
The API fallback was also exercised through a real child after abs404.

Product 0.1.22 selects sequence 22, Control schema 19 and qualified Hermes
generation 9. No dependencies, credentials, database migrations, metadata-cache
schema or cross-machine request coordinator are added. Existing captures are
not automatically resubmitted by this release.

## Installed acceptance

- Built source: `5d87288e1cd3cd812902c390c58a2fbba3f276be`.
- Installed generation: `cortex-research-22-53e68206bc785877`.
- Hosted run `34906178656` passed all four jobs at the built source.
- Source checks: 326 engine/research and 394 Control/artifact/release checks.
- Both bundle verifiers and a fresh embedded-Python installation passed. The
  real child indexed seven chunks via direct abs/HTML without any API request;
  permanent metadata failure remained known and refused corpus writes.
- A fresh Media state archive and paired recovery proof preceded the official
  same-schema upgrade. Configuration digest, seven native bindings and the
  qualified worker pointer were preserved. New Web and bound-worker health
  passed; original runtime and Telegram activation gates were restored.
- A read-only query on the upgraded host retrieved a real paper's metadata and
  HTML with two HTTP200 requests, zero export API requests, and 50,183 parsed
  Markdown characters. Metadata took 0.756 seconds and metadata plus HTML
  parsing took 1.708 seconds in that observation. Timing is not a latency SLA.

The live query did not download figure files or write the corpus; end-to-end
production capture acceptance and fresh model/message delivery were not rerun.
The previous generation remains installed. PDF-only ingestion and cross-process
request coordination remain separate work.
