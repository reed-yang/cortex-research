# Reuse the same original encrypted refresh token on two fresh hosted VMs.
name: agy OAuth check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    if: github.ref_name == github.event.repository.default_branch
    runs-on: ubuntu-latest
    environment: pr-review
    timeout-minutes: 8
    strategy:
      fail-fast: true
      max-parallel: 1
      matrix:
        session: [1, 2]
    concurrency:
      group: agy-subscription-account
      cancel-in-progress: false
    steps:
      - name: Verify native renewal and a single review turn
        uses: reed-yang/independent-pr-review@d5a3d8eb12be4ee73de934c69ae37077b9cb434e
        with:
          phase: auth-check
          output: ${{ runner.temp }}/review-auth-check
        env:
          AGY_OAUTH_JSON: ${{ secrets.AGY_OAUTH_JSON }}
          GEMINI_MODEL: ${{ vars.AGY_MODEL }}
          GEMINI_EFFORT: ${{ vars.GEMINI_EFFORT }}
          GEMINI_CONTEXT_WINDOW: ${{ vars.GEMINI_CONTEXT_WINDOW }}
